#pragma once

#include <string>
using namespace std;

// This header file just has the declaration for each function, it lets
// the Main.cpp source file call the functions while the actual code or
// implementation for each of these functions is in ToComplete.cpp

string reverseString( string toReverse );

int sumRange(int low, int high);
